#include <stdio.h>
#include <math.h>
#include <conio.h>

void main(void)
{
	//Perimeter and Squre
	/*double side, P, S;

	printf("Side of a square: ");
	scanf("%lf", &side);

	P = 4 * side;
	S = a * side;

	printf("\nP = %0*.*lf\nS = %0*.*lf", 0, 1, P, 0, 1, S);*/

	//

	_getch();
}